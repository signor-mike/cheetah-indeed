const config = {
    // chrome, internet explorer, edge, firefox, safari
    browser: "firefox",
    // credentials are not required. They'll be logged into terminal if provided.
    login: { username: "dev_mike.k@yahoo.com", password: "" },
    // job details
    jobDetails: {
        // job title
        title: "frontend developer",
        // location, exactly as on indeed.
        location: "remote",
    },
};

export default config;
